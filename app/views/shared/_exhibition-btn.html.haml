- if user_signed_in?
  -if current_user.genre == 1
    .exhibition.dv
      = link_to new_space_path  do
        = icon('fas', 'city') 
        %p 区画登録
  - if current_user.genre == 2
    .exhibition.te
      = link_to new_brand_path do
        = icon('fas', 'store')
        %p.te 業態登録